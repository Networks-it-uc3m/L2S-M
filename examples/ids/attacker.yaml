apiVersion: v1
kind: Pod
metadata:
  name: attacker
  labels:
    l2sm/app: attacker
    l2sm: "true"
  annotations:
    l2sm/networks: '[{"name": "ztna-example-1-portscan", "ips": ["10.0.0.100/24"]}]'
spec:
  containers:
  - name: alpine
    # Installs nmap on startup, then keeps the container running
    command: ["/bin/ash", "-c", "apk update && apk add nmap && trap : TERM INT; sleep infinity & wait"]
    image: alpine:latest
