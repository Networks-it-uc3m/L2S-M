
apiVersion: "k8s.cni.cncf.io/v1"
kind: NetworkAttachmentDefinition
metadata:
  name: veth8
  namespace: l2sm-system
  labels:
    app: l2sm
spec:
  config: '{
      "cniVersion": "0.3.0",
      "type": "bridge",
      "bridge": "br8",
      "mtu": 1400,
      "device": "veth8",
        "ipam": {
          "type":"static"
        }
    }'
