
apiVersion: "k8s.cni.cncf.io/v1"
kind: NetworkAttachmentDefinition
metadata:
  name: veth7
  labels:
    app: l2sm
spec:
  config: '{
      "cniVersion": "0.3.0",
      "type": "bridge",
      "bridge": "br7",
      "mtu": 1400,
      "device": "veth7",
        "ipam": {
          "type":"static"
        }
    }'
